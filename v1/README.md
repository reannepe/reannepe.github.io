# v1

First portfolio variant for Reanne Pe, preserved from the original static subdomain implementation.

## GitHub Pages-style testing

- Entry path: `/v1/`
- Site files: `public/`
- Redirect shim: `index.html` forwards `/v1/` to `/v1/public/`

## Notes

- This variant is kept separate from `v2` for side-by-side testing.
- Image and PDF assets remain under `public/images/`.
