# v2

Second portfolio variant for Reanne Pe, curated more tightly around architectural visualization and presentation quality.

## GitHub Pages-style testing

- Entry path: `/v2/`
- Site files: `public/`
- Redirect shim: `index.html` forwards `/v2/` to `/v2/public/`

## Notes

- This variant stays isolated from `v1` for testing.
- Curated assets live in `public/images/selected/`.
- Drawing previews live in `public/images/plans/`.
