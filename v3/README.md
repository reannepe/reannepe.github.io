# v3

Third portfolio variant for Reanne Pe, combining the cleaner structure from `v1` with the more focused positioning from `v2`.

## GitHub Pages-style testing

- Entry path: `/v3/`
- Site files: `public/`
- Redirect shim: `index.html` forwards `/v3/` to `/v3/public/`

## Notes

- This variant groups gallery browsing more tightly by work type.
- `Featured Projects` uses placeholder detail popups until final project folders and captions are supplied.
- Software claims are intentionally limited to Reanne's current strongest tools.
